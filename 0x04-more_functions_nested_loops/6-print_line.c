#include "main.h"
/**
 * main - Entry point of the program
 *
 * This program demonstrates the usage of an if-else statement within a while loop.
 * It prints whether each iteration of the loop reached the value 3 or not.
 *
 * Return: Always 0 (success)
 */
void print_line(int n)
{
	int i = 1;

	for (i = 1; i <= n; i++)
	{
		_putchar('_');

	}
	_putchar('\n');
}
