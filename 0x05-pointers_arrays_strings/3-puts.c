#include "main.h"
#include <stdio.h>
/**
 * _puts - Prints a string followed by a newline to stdout
 * @str: Pointer to the string to be printed
 *
 * This function prints the string pointed to by 'str'
 * to the standard output, followed by a newline character ('\n').
 */
void _puts(char *str)
{
	puts(str);
}
